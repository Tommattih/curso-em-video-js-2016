function soma (n1=0, n2=0) { // <----- em caso de parametros faltando ou vazios, setar uma prevenção de erros [no caso, soma o parametro inserido + zero]
    return n1 + n2
}
/*let res = parimpar(187)
console.log(res) */
console.log(soma(7))